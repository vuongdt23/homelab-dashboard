# Copy to .env and fill in your homelab values. .env is gitignored.

# The host/IP where your services listen (Tailscale IP, LAN IP, or hostname).
# This is the address the dashboard health-checks and links the cards to.
DASHBOARD_HOST=100.64.0.1

# Port the dashboard itself serves on.
DASHBOARD_PORT=8770
